# Copy to .env.production before `npm run build` in CI/Docker.
# NEXT_PUBLIC_* is baked into the client bundle at build time — get it right.
NEXT_PUBLIC_API_URL=https://api.domain.com
API_URL=https://api.domain.com
# REVALIDATE_SECRET is read at runtime — set it in cPanel's Node app env panel.
